rules_version = '2';

// Deny by default. Owner-scoped rules for questions/quizzes land in P1/P2;
// never deploy Firebase's world-open "test mode" rules.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
