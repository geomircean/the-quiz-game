# Loaded ONLY by `next dev` (never by `next build`), so the emulator wiring
# and the __QUIZ_DEV__ test hook can never reach a production bundle — the
# postbuild guard (scripts/assert-clean-bundle.mjs) enforces that too.
NEXT_PUBLIC_FIREBASE_USE_EMULATORS=true
